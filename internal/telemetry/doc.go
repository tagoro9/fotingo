// Package telemetry provides privacy-safe analytics instrumentation for the CLI.
//
// It centralizes event schema enforcement, event emission, and integration-call
// instrumentation so command packages can emit telemetry without handling
// transport and redaction details directly.
package telemetry
